import React from 'react'
import { DexSwap } from '../../components'

type TradeProps = {
    data : String
}

const Trade = (props: TradeProps) => {
  return <div><DexSwap /></div>
}

export default Trade
